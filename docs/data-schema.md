# Data Schema Stabilization

This document defines the current, enforced rules for stable internal IDs and human-readable slugs in JSON data under src/data.

## Scope

- Applies to all JSON files in src/data.
- IDs and slugs are generated by scripts/add-stable-ids-and-slugs.js.
- Existing id and slug values are preserved. Missing values are backfilled.

## ID Format

Format:

- <fileBase>_<hash12>

Example:

- greekRestaurants_7b449c4e0fce

Where:

- fileBase is the JSON filename without extension.
- hash12 is the first 12 hex chars of SHA-256 over an identity seed.

Identity seed format:

- <fileBase>|<contextPath>|<pieces...>

Pieces are collected in this exact key order when non-empty:

1. name
2. title
3. question
4. key
5. region
6. address
7. url
8. link
9. description
10. lat (if present)
11. lng (if present)

Fallback seed when pieces are empty:

- <fileBase>|<contextPath>

Notes:

- contextPath is the traversal path in the JSON tree (for arrays it includes index positions).
- IDs are internal and opaque. Do not derive UI labels from IDs.

## Slug Rules

Slug is added only when slug is missing.

Source label precedence:

1. name
2. title
3. question
4. key
5. region
6. address
7. id

Readable slug generation:

1. Transliterate Greek to Latin (mapping in script).
2. Unicode normalize to NFD.
3. Remove combining marks.
4. Lowercase.
5. Replace non [a-z0-9] sequences with hyphen.
6. Trim leading/trailing hyphens.
7. Collapse repeated hyphens.

Final slug format:

- <readable>-<hash4>

Example:

- ta-platania-7b44

If readable part becomes empty, use:

- item

## Transliteration Rules

The transliteration is deterministic and character-based.

Examples:

- θ -> th
- χ -> ch
- ψ -> ps
- υ -> y
- η -> i
- ς -> s
- ά -> a
- έ -> e
- ή -> i

Uppercase Greek letters are also mapped and then lowercased by slug generation.

Source of truth for the full mapping:

- scripts/add-stable-ids-and-slugs.js

## Collision Handling

Slug collision handling is per file.

Process:

1. Create default slug: <readable>-<hash4>.
2. If already used in same file, extend suffix to 8 chars: <readable>-<hash8>.

Important:

- Collision checks are local to each JSON file.
- The hash is derived from the same identity digest used for IDs.

## Stability Guarantees

Guaranteed stable on repeated runs when records already have id and slug.

For records missing id/slug at generation time:

- Stability depends on identity seed inputs.
- If seed inputs change (including contextPath), generated values may differ.

Practical guidance:

- Treat id and slug as write-once fields after first generation.
- Do not delete id/slug during content edits.
- For enrichment datasets, join on id.

## Enrichment Infrastructure Guidance

## Canonical Type Layer

Phase one introduces a shared TypeScript contract for location-like records:

- `src/types/location.ts`

Purpose:

- centralize the platform's location shapes in one place
- give UI, scripts, and future validators one common contract
- keep enrichment joins anchored on stable `id`

Current exported contracts:

- `LocationBase`
- `RestaurantLocation`
- `MunicipalityLocation`
- `AttractionLocation`
- `DiscoverableLocation`
- `LocationEnrichment`

Pragmatic migration rule:

- existing raw JSON can be adapted into these types at the loading boundary
- do not spread one-off local interfaces across components for the same location concept
- future runtime validation should mirror these contracts instead of redefining them

For AI enrichment fields (summaries, tags, synonyms, embeddings, relations):

- Keep base fields backward-compatible.
- Attach enrichment by stable id.
- Prefer separate enrichment artifacts keyed by id for large semantic payloads.

Suggested pattern:

- Base JSON remains in src/data.
- Enrichment files include record_id equal to base id.
- Slug can be used for readable URL paths, but id is the canonical join key.

## Operational Commands

Generate IDs/slugs:

- npm run data:stabilize

Copy source data to public/data:

- npm run prebuild

## Related Files

- scripts/add-stable-ids-and-slugs.js
- package.json
- src/data
- public/data
