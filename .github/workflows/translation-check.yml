name: translations-check

on:
  workflow_dispatch: {}

jobs:
  noop:
    name: Disabled - noop
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "translations-check workflow disabled by repository maintenance"
